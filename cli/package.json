{
  "name": "thirdman",
  "version": "0.1.0",
  "private": true,
  "description": "A merchant's own codebase auditor: reads a real repo, scores what an AI buyer can and cannot do with the store, and offers to write the integration as a diff the merchant approves. See plans/layer-20-merchant-cli.md.",
  "type": "module",
  "bin": {
    "thirdman": "./bin/thirdman.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "prompts": "^2.4.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "@types/node": "^20",
    "@types/prompts": "^2.4.9",
    "vitest": "^3.0.0"
  }
}
